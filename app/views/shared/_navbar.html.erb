<!-- Header -->
<header class="border-b border-yellow-500/20 fixed w-full z-50 transition-all duration-300 py-2 bg-[#0F172A]">
  <div class="container mx-auto px-4 flex items-center text-gray-500" id="logo-text">
    <div class="flex items-center flex-shrink-0">
      <div class="flex-shrink-0 flex items-center">
        <i data-lucide="activity" class="h-8 w-8 text-red-500"></i>
        <%= link_to root_path, class: "flex items-center space-x-2" do %>
          <span class="ml-2 text-xl font-bold bg-gradient-to-r from-red-500 to-yellow-500 bg-clip-text text-transparent">
            chainfetch.app
          </span>
        <% end %>
      </div>
    </div>
    
    <!-- Search Dropdown -->
    <div class="hidden md:flex flex-1 mx-4 sm:mx-8 relative" id="search-container">
      <div class="relative w-full">
        <input 
          type="text" 
          id="search-input"
          placeholder="Search blocks, transactions, addresses, smart contracts and tokens" 
          class="w-full px-4 py-2 pl-10 pr-4 bg-[#1E293B]/80 border border-yellow-500/20 rounded-lg text-gray-200 placeholder-gray-400 focus:outline-none focus:border-yellow-500/60 focus:bg-[#1E293B] transition-all duration-200"
        />
        <svg xmlns="http://www.w3.org/2000/svg" class="absolute left-3 top-2.5 h-5 w-5 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
      </div>
      
      <!-- Search Dropdown Menu -->
      <div id="search-dropdown" class="absolute top-full left-0 right-0 mt-1 bg-[#1E293B] border border-yellow-500/20 rounded-lg shadow-xl backdrop-blur-sm z-50 hidden">
        <div class="p-2">
          <div class="text-xs text-gray-400 px-3 py-2 font-medium">SEARCH CATEGORIES</div>
          <div class="space-y-1">
            <button class="search-category w-full text-left px-3 py-2 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="blocks">
              <div class="w-8 h-8 bg-blue-500/20 rounded-lg flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14-7H5a2 2 0 00-2 2v12a2 2 0 002 2h14a2 2 0 002-2V6a2 2 0 00-2-2z" />
                </svg>
              </div>
              <div>
                <div class="text-gray-200 font-medium">Blocks</div>
                <div class="text-xs text-gray-400">Search Ethereum blocks</div>
              </div>
            </button>
            
            <button class="search-category w-full text-left px-3 py-2 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="transactions">
              <div class="w-8 h-8 bg-green-500/20 rounded-lg flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" />
                </svg>
              </div>
              <div>
                <div class="text-gray-200 font-medium">Transactions</div>
                <div class="text-xs text-gray-400">Search Ethereum transactions</div>
              </div>
            </button>
            
            <button class="search-category w-full text-left px-3 py-2 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="addresses">
              <div class="w-8 h-8 bg-purple-500/20 rounded-lg flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-purple-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                </svg>
              </div>
              <div>
                <div class="text-gray-200 font-medium">Addresses</div>
                <div class="text-xs text-gray-400">Search Ethereum addresses</div>
              </div>
            </button>
            
            <button class="search-category w-full text-left px-3 py-2 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="smart-contracts">
              <div class="w-8 h-8 bg-orange-500/20 rounded-lg flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-orange-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                </svg>
              </div>
              <div>
                <div class="text-gray-200 font-medium">Smart Contracts</div>
                <div class="text-xs text-gray-400">Search verified contracts</div>
              </div>
            </button>
            
            <button class="search-category w-full text-left px-3 py-2 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="tokens">
              <div class="w-8 h-8 bg-red-500/20 rounded-lg flex items-center justify-center">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-red-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1" />
                </svg>
              </div>
              <div>
                <div class="text-gray-200 font-medium">Tokens</div>
                <div class="text-xs text-gray-400">Search ERC-20, ERC-721 tokens</div>
              </div>
            </button>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Mobile Search Button -->
    <div class="md:hidden">
      <button id="mobile-search-btn" class="p-2 text-gray-400 hover:text-gray-200 transition-colors">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
      </button>
    </div>
    
    <nav class="hidden sm:flex space-x-8 justify-end flex-shrink-0">
      <% unless controller_path.include?('app') || controller_path.include?("oas_rails") %>
        <%= link_to "Features", "/#core-features", class: "mt-3 hover:text-yellow-400 transition-colors" %>
        <%= link_to "API", "/#semantic-search-endpoint", class: "mt-3 hover:text-yellow-400 transition-colors" %>
        <%= link_to "Architecture", "/#architecture", class: "mt-3 hover:text-yellow-400 transition-colors" %>
        <%= link_to "Use Cases", "/#use-cases", class: "mt-3 hover:text-yellow-400 transition-colors" %>
        <%= link_to "Docs", "/docs", class: "mt-3 hover:text-yellow-400 transition-colors" %>
      
        <%= link_to "/app", class: "group relative px-6 py-3 bg-gradient-to-r from-yellow-500 to-red-500 text-white rounded-md font-semibold transition-all duration-200 hover:shadow-[0_0_20px_rgba(234,179,8,0.3)] overflow-hidden inline-block" do %>
          <span class="relative z-10">Get API Key</span>
          <span class="absolute inset-0 bg-gradient-to-r from-red-500 to-yellow-500 transition-transform duration-200 translate-x-full group-hover:translate-x-0"></span>
        <% end %>
      <% end %>
    </nav>
    
    <% if controller_path.include?('app') || controller_path.include?("oas_rails") %>
      <div class="flex items-center">
        <div class="hidden sm:flex items-center space-x-4 mr-4">
          <%= link_to "Alerts", app_ethereum_alerts_path, class: "text-gray-400 hover:text-gray-200 transition-colors" %>
          <%= link_to "Documentation", "/docs", class: "text-gray-400 hover:text-gray-200 transition-colors" %>
          <%= link_to "Dashboard", app_dashboard_index_path, class: "text-gray-400 hover:text-gray-200 transition-colors" %>
        </div>
        <div class="inline-flex items-center px-2 py-2 rounded-md bg-[#1E293B]/80 border border-yellow-500/20 shadow-lg backdrop-blur-sm mr-4" id="user_credit">
          <%= link_to app_dashboard_index_path, class: "flex items-center space-x-1 hover:cursor-pointer" do %>
            <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6 text-yellow-500" fill="currentColor" viewBox="0 0 20 20">
              <path d="M4 4h12a2 2 0 012 2v1H2V6a2 2 0 012-2zm14 4v6a2 2 0 01-2 2H4a2 2 0 01-2-2V8h16zm-2 3a1 1 0 10-2 0 1 1 0 002 0z"/>
            </svg>
            <span class="ml-2 text-lg font-bold bg-gradient-to-r from-red-500 to-yellow-500 bg-clip-text text-transparent">
              <% credits = Current.user.api_credit %>
              <%= credits.present? ? number_with_delimiter(credits.to_f.round(2)) : "–" %> Credits
            </span>
          <% end %>
        </div>
        <%= button_to session_path, method: :delete, form: {data: {turbo_confirm: 'Are you sure?'}}, class: "flex items-center px-4 py-2 bg-transparent border border-yellow-500/30 font-semibold rounded-md hover:border-yellow-500/60 hover:bg-yellow-500/10 hover:cursor-pointer transition-all duration-200" do %>
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="w-4 h-4 mr-2"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" x2="9" y1="12" y2="12"></line></svg>  
          Logout
        <% end %>
      </div>
    <% else %>
      <%= link_to "Get API Key", "/app", class: "group relative px-6 py-3 bg-gradient-to-r from-yellow-500 to-red-500 text-white rounded-md font-semibold transition-all duration-200 hover:shadow-[0_0_20px_rgba(234,179,8,0.3)] overflow-hidden inline-block sm:hidden" %>
    <% end %>
  </div>
</header>

<!-- Mobile Search Modal -->
<div id="mobile-search-modal" class="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 hidden md:hidden">
  <div class="flex items-start justify-center min-h-screen pt-16 px-4">
    <div class="bg-[#1E293B] border border-yellow-500/20 rounded-lg shadow-xl w-full max-w-md">
      <div class="p-4">
        <div class="flex items-center justify-between mb-4">
          <h3 class="text-lg font-semibold text-gray-200">Search</h3>
          <button id="close-mobile-search" class="text-gray-400 hover:text-gray-200">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
        
        <div class="relative mb-4">
          <input 
            type="text" 
            id="mobile-search-input"
            placeholder="Enter your search query..." 
            class="w-full px-4 py-3 pl-10 pr-4 bg-[#0F172A] border border-yellow-500/20 rounded-lg text-gray-200 placeholder-gray-400 focus:outline-none focus:border-yellow-500/60 transition-all duration-200"
          />
          <svg xmlns="http://www.w3.org/2000/svg" class="absolute left-3 top-3.5 h-5 w-5 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
          </svg>
        </div>
        
        <div class="space-y-2">
          <div class="text-xs text-gray-400 px-1 py-1 font-medium">SELECT CATEGORY</div>
          <button class="mobile-search-category w-full text-left px-3 py-3 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="blocks">
            <div class="w-8 h-8 bg-blue-500/20 rounded-lg flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14-7H5a2 2 0 00-2 2v12a2 2 0 002 2h14a2 2 0 002-2V6a2 2 0 00-2-2z" />
              </svg>
            </div>
            <div>
              <div class="text-gray-200 font-medium">Blocks</div>
              <div class="text-xs text-gray-400">Search Ethereum blocks</div>
            </div>
          </button>
          
          <button class="mobile-search-category w-full text-left px-3 py-3 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="transactions">
            <div class="w-8 h-8 bg-green-500/20 rounded-lg flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7h12m0 0l-4-4m4 4l-4 4m0 6H4m0 0l4 4m-4-4l4-4" />
              </svg>
            </div>
            <div>
              <div class="text-gray-200 font-medium">Transactions</div>
              <div class="text-xs text-gray-400">Search Ethereum transactions</div>
            </div>
          </button>
          
          <button class="mobile-search-category w-full text-left px-3 py-3 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="addresses">
            <div class="w-8 h-8 bg-purple-500/20 rounded-lg flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-purple-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
              </svg>
            </div>
            <div>
              <div class="text-gray-200 font-medium">Addresses</div>
              <div class="text-xs text-gray-400">Search Ethereum addresses</div>
            </div>
          </button>
          
          <button class="mobile-search-category w-full text-left px-3 py-3 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="smart-contracts">
            <div class="w-8 h-8 bg-orange-500/20 rounded-lg flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-orange-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
              </svg>
            </div>
            <div>
              <div class="text-gray-200 font-medium">Smart Contracts</div>
              <div class="text-xs text-gray-400">Search verified contracts</div>
            </div>
          </button>
          
          <button class="mobile-search-category w-full text-left px-3 py-3 rounded-md hover:bg-yellow-500/10 transition-colors flex items-center space-x-3" data-category="tokens">
            <div class="w-8 h-8 bg-red-500/20 rounded-lg flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-red-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1" />
              </svg>
            </div>
            <div>
              <div class="text-gray-200 font-medium">Tokens</div>
              <div class="text-xs text-gray-400">Search ERC-20, ERC-721 tokens</div>
            </div>
          </button>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="h-12"></div>

<script>
  // Header scroll effect
  window.addEventListener('scroll', () => {
    const header = document.querySelector('header');
    if (window.scrollY > 10) {
      header.classList.add('bg-white', 'shadow-md');
      header.classList.remove('bg-[#0F172A]');
    } else {
      header.classList.remove('bg-white', 'shadow-md');
      header.classList.add('bg-[#0F172A]');
    }
  });

  // Search dropdown functionality
  document.addEventListener('turbo:load', () => {
    const searchInput = document.getElementById('search-input');
    const searchDropdown = document.getElementById('search-dropdown');
    const searchCategories = document.querySelectorAll('.search-category');
    
    // Mobile search elements
    const mobileSearchBtn = document.getElementById('mobile-search-btn');
    const mobileSearchModal = document.getElementById('mobile-search-modal');
    const closeMobileSearch = document.getElementById('close-mobile-search');
    const mobileSearchInput = document.getElementById('mobile-search-input');
    const mobileSearchCategories = document.querySelectorAll('.mobile-search-category');
    
    let selectedCategory = null;
    let mobileSelectedCategory = null;

    // Populate search input with query parameter if it exists
    const urlParams = new URLSearchParams(window.location.search);
    const queryParam = urlParams.get('query');
    if (queryParam && searchInput) {
      searchInput.value = queryParam;
    }
    if (queryParam && mobileSearchInput) {
      mobileSearchInput.value = queryParam;
    }

    // Show dropdown on input focus
    searchInput.addEventListener('focus', () => {
      searchDropdown.classList.remove('hidden');
    });

    // Hide dropdown when clicking outside
    document.addEventListener('click', (e) => {
      if (!document.getElementById('search-container').contains(e.target)) {
        searchDropdown.classList.add('hidden');
      }
    });

    // Handle category selection
    searchCategories.forEach(category => {
      category.addEventListener('click', () => {
        selectedCategory = category.dataset.category;
        const categoryName = category.querySelector('.text-gray-200').textContent;
        
        // Update input placeholder
        searchInput.placeholder = `Search ${categoryName.toLowerCase()}...`;
        
        // Visual feedback for selected category
        searchCategories.forEach(cat => cat.classList.remove('bg-yellow-500/20'));
        category.classList.add('bg-yellow-500/20');
        
        // Focus back to input
        searchInput.focus();
      });
    });

    // Handle search input
    searchInput.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') {
        const query = searchInput.value.trim();
        if (query && selectedCategory) {
          performSearch(selectedCategory, query);
        } else if (query) {
          // If no category selected, show dropdown
          searchDropdown.classList.remove('hidden');
        }
      }
    });

    // Perform search based on category
    function performSearch(category, query) {
      let searchType = '';
      
      switch(category) {
        case 'blocks':
          searchType = 'blocks';
          break;
        case 'transactions':
          searchType = 'transactions';
          break;
        case 'addresses':
          searchType = 'addresses';
          break;
        case 'smart-contracts':
          searchType = 'smart_contracts';
          break;
        case 'tokens':
          searchType = 'tokens';
          break;
      }
      
      if (searchType) {
        // Use Turbo to navigate to search page
        const searchUrl = `/app/ethereum/${searchType}/search?query=${encodeURIComponent(query)}`;
        Turbo.visit(searchUrl);
        
        // Hide dropdown and clear input
        searchDropdown.classList.add('hidden');
        searchInput.value = '';
        searchInput.placeholder = 'Search blocks, transactions, addresses, smart contracts and tokens';
        
        // Reset category selection
        searchCategories.forEach(cat => cat.classList.remove('bg-yellow-500/20'));
        selectedCategory = null;
      }
    }

    // Show search suggestions on input
    searchInput.addEventListener('input', (e) => {
      const query = e.target.value.trim();
      if (query.length > 0) {
        searchDropdown.classList.remove('hidden');
      }
    });

    // Mobile search functionality
    if (mobileSearchBtn) {
      mobileSearchBtn.addEventListener('click', () => {
        mobileSearchModal.classList.remove('hidden');
        setTimeout(() => mobileSearchInput.focus(), 100);
      });
    }

    if (closeMobileSearch) {
      closeMobileSearch.addEventListener('click', () => {
        mobileSearchModal.classList.add('hidden');
        mobileSearchInput.value = '';
        mobileSelectedCategory = null;
        mobileSearchCategories.forEach(cat => cat.classList.remove('bg-yellow-500/20'));
      });
    }

    // Close mobile modal on backdrop click
    if (mobileSearchModal) {
      mobileSearchModal.addEventListener('click', (e) => {
        if (e.target === mobileSearchModal) {
          mobileSearchModal.classList.add('hidden');
          mobileSearchInput.value = '';
          mobileSelectedCategory = null;
          mobileSearchCategories.forEach(cat => cat.classList.remove('bg-yellow-500/20'));
        }
      });
    }

    // Handle mobile category selection
    mobileSearchCategories.forEach(category => {
      category.addEventListener('click', () => {
        const query = mobileSearchInput.value.trim();
        if (query) {
          // Perform search immediately if there's a query
          performSearch(category.dataset.category, query);
          mobileSearchModal.classList.add('hidden');
          mobileSearchInput.value = '';
          mobileSelectedCategory = null;
          mobileSearchCategories.forEach(cat => cat.classList.remove('bg-yellow-500/20'));
        } else {
          // Just select the category
          mobileSelectedCategory = category.dataset.category;
          const categoryName = category.querySelector('.text-gray-200').textContent;
          
          // Update input placeholder
          mobileSearchInput.placeholder = `Search ${categoryName.toLowerCase()}...`;
          
          // Visual feedback for selected category
          mobileSearchCategories.forEach(cat => cat.classList.remove('bg-yellow-500/20'));
          category.classList.add('bg-yellow-500/20');
          
          // Focus back to input
          mobileSearchInput.focus();
        }
      });
    });

    // Handle mobile search input
    if (mobileSearchInput) {
      mobileSearchInput.addEventListener('keydown', (e) => {
        if (e.key === 'Enter') {
          const query = mobileSearchInput.value.trim();
          if (query && mobileSelectedCategory) {
            performSearch(mobileSelectedCategory, query);
            mobileSearchModal.classList.add('hidden');
            mobileSearchInput.value = '';
            mobileSelectedCategory = null;
            mobileSearchCategories.forEach(cat => cat.classList.remove('bg-yellow-500/20'));
          }
        }
      });
    }
  });
</script>